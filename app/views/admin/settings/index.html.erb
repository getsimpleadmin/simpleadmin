<section class="item-page">
  <header class="item-page__header">
      <h3 class="item-page__title">Настройки</h3>
  </header>

  <% @settings.each do |setting| %>
    <%= render 'admin/settings/form', resource: setting, form_url: admin_setting_path(setting, locale: I18n.locale) %>
  <% end %>
</section>

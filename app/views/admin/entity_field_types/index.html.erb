<section class="catalog-page">
  <header class="item-page__header">
    <h3 class="item-page__title"><%= t('.title') %></h3>
    <%= link_to t('admin.create_button'), new_admin_entity_field_type_path, class: 'button item-page__submit' %>
  </header>

  <table class="table">
    <%= render 'admin/shared/table_header', resource_settings: @resource_settings %>
    <%= render 'admin/shared/table_body', resources: @entity_field_types %>
  </table>
</section>

<thead>
    <tr>
        <% column_names.each do |column_name| %>
          <th class="<%= 'text-center' if column_names.first == column_name %>">
            <%= column_name %>
          </th>
        <% end %>

        <th class="text-center">Actions</th>
    </tr>
</thead>

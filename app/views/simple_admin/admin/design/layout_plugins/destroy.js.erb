var resourceId = "<%= @resource.id %>";

$('*[data-layout-plugin-id="' + resourceId + '"]').remove();

$.notify(
  'Layout plugin successfully deleted!',
  'success',
  { autoHideDelay: 3000, position: 'right' }
);

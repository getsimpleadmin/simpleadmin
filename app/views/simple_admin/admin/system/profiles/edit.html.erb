<section class="item-page">
  <header class="item-page__header">
      <h3 class="item-page__title">Edit profile</h3>
  </header>

  <%= form_for current_user, url: admin_system_profile_path(current_user) do |f| %>
    <section class="field">
        <div class="field__title">Email</div>
        <div class="field__content">
          <%= f.text_field :email, class: 'input' %>
        </div>
    </section>

    <%= f.fields_for :profile do |pf| %>
      <section class="field">
          <div class="field__title">First Name</div>
          <div class="field__content">
            <%= pf.text_field :first_name, class: 'input' %>
          </div>
      </section>

      <section class="field">
          <div class="field__title">Last Name</div>
          <div class="field__content">
            <%= pf.text_field :last_name, class: 'input' %>
          </div>
      </section>

      <section class="field">
          <div class="field__title">Avatar</div>
          <div class="field__content">
              <label class="image__label">
                  <%= pf.file_field :avatar, class: 'image' %>
              </label>
          </div>
      </section>
    <% end %>

    <section class="field">
      <div class="field__title"></div>
      <div class="field__content">
        <%= f.submit class: 'button button--wide item-page__button' %>
      </div>
    </section>
  <% end %>
</section>

<%= form_for resource, url: form_url, html: { class: 'form-horizontal' } do |f| %>
  <section class="field">
      <div class="field__title">On development stage..</div>
      <div class="field__content">
      </div>
  </section>
<% end %>
